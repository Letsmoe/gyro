import {colarg} from "./index.js";

const args = colarg(process.argv.slice(2))
			.command("new", "Function",(...arrArgs : any[]) => {
				console.log(arrArgs);
			})
			.option("name", "n", "jeff", "Some description", true, "string")
			.help().args;